<h1>Listing fleets</h1>

<table>
  <tr>
    <th>Color</th>
  </tr>

<% @fleets.each do |fleet| %>
  <tr>
    <td><%=h fleet.color %></td>
    <td><%= link_to 'Show', fleet %></td>
    <td><%= link_to 'Edit', edit_fleet_path(fleet) %></td>
    <td><%= link_to 'Destroy', fleet, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New fleet', new_fleet_path %>