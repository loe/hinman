<% page_title 'Rotations' %>

<h1>Listing Rotations</h1>

<table>
  <tr>
    <th>Name</th>
    <th></th>
  </tr>

<% @rotations.each do |rotation| %>
  <tr>
    <td><%= rotation.name %></td>
    <td><%= link_to 'Show', rotation %> <%= link_to 'Edit', edit_rotation_path(rotation) %> <%= link_to 'Destroy', rotation, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New rotation', new_rotation_path %>